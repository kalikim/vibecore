services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_PASSWORD: vibecore-fixture-only
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 2s
      timeout: 2s
      retries: 15
